# Leeyes Unicode修復 進捗ログ

## 結論: 解決しました

P9NPクラッシュの根本原因を突き止め、**管理者権限もPC再起動も不要**な形で
inject_dll.dll自体に修正を組み込みました。`LeeyesW.exe` を普通にダブルクリック
するだけで、ユーザー報告のフォルダを含め、Unicode名のフォルダ/画像/書庫が
問題なく開けることを確認済みです。

## 最終的な修正内容

1. **p9np.dllのプロセスへの読み込みをブロック**
   `kernel32.dll` の `LoadLibraryA/W/ExA/ExW` と `ntdll.dll` の `LdrLoadDll`
   (内部コンポーネントが実際に使っている低レベルAPI)をフックし、
   p9np.dllの読み込み要求だけを失敗させる。

2. **このプロセスから見えるレジストリ値を書き換え**
   `advapi32.dll` の `RegQueryValueExW` をフックし、
   `NetworkProvider\Order` を読み取る際にP9NPを除いた値を返す。
   **レジストリ自体への書き込みは一切行わない**(このプロセス内で
   読み取り結果だけを差し替えるので、システムの他の部分・他のプロセスには
   一切影響しない)。

これにより、p9np.dll内部の不具合が誘発される経路そのものを断ち、
管理者権限を必要とする `Launch_Leeyes_Unicode.bat` は不要になった
(念のためフォルダには残してあるが、使わなくてよい)。

## 検証結果

以下すべてで安定動作を確認(各6〜8回連続実行、全てクラッシュなし):
- 単純なASCIIフォルダ
- 日本語のみのフォルダ名
- 全角記号(，！)を含むフォルダ名
- 簡体字中国語を含むフォルダ名
- **ユーザー報告の実フォルダ**:
  `E:\download\jav\260829\[NCDX]群U定制要看兽，笑面教授！ [AI Generated]`
  (再現用に同名フォルダをE:上の別ディレクトリに作成してテスト)
- 管理者権限なし・特別なランチャーなしでの通常起動(`LeeyesW.exe`を直接実行)

## 副次的に見つかった別の問題と対応

調査の過程で、8.3短縮ファイル名が無効化されたドライブ(E:がこれに該当)では、
このツール本来の仕組み(8.3短縮パスへのフォールバック)が機能しないことが
分かった。対策として、8.3の代わりにASCII安全な名前のジャンクション/
ハードリンクを自動生成するフォールバック機構をソースに実装済み。
既定では無効(`inject_dll.ini` の `HookFindFirstFile=0`)のまま据え置いた
(P9NP問題が解決した今、有効化の検証は改めて可能。まずは現状の修正で
報告のあった問題が解決するか確認してから、必要であれば次のステップとする)。

## テスト時に整理したもの

調査用に作成した一時ファイル・テストフォルダ(E:\_leeyes_selftest 等)は
すべて削除済み。本番の `LeeyesW.exe` / `inject_dll.dll` は最終版に更新し、
`ReadMe.UnicoeHack.txt` にも今回の修正内容を反映済み。
